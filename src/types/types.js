// @flow

export type Courses = {
    id: number,
    title: string,
    slug: string,
    authorId: number,
    category: string,
    authorName: string,
};

export type CourseObj = {
    id: number,
    title: string,
    authorId: number,
    category: string
}
